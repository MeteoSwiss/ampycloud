#
# Copyright (c) 2026 MeteoSwiss, created by F.P.A. Vogt; frederic.vogt@meteoswiss.ch

name: CI_speed_check

on:
  pull_request:
    branches: [ main ]

jobs:
  speed_check:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ['3.10', '3.11','3.12']

    steps:
      # Checkout our repository
      - name: Checkout current repository
        uses: actions/checkout@v4

      # Set up Python
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          python -m pip install poetry

      - name: Install dependencies
        run: poetry install

      # Launch a home-made python script to check the speed
      # Always run Python scripts via 'poetry run' to use the correct environment
      - name: Run the speed test
        run: poetry run python .github/workflows/speed_check.py
        shell: bash
